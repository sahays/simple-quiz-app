const QuestionStore = () => {
  const createNewQuestion = ({
    id,
    question,
    choices,
    answers,
    explanation,
    questionType,
    tags,
  }) => {
    // create a new Question
    // create a new tag
    // associate question with tag
    return;
  };

  return {
    createNewQuestion,
  };
};

export default QuestionStore;
